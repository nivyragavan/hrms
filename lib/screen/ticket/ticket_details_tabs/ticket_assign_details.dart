import 'package:flutter/material.dart';

class TicketAssignDetails extends StatelessWidget {
  const TicketAssignDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
