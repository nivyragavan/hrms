import 'package:flutter/material.dart';

class TicketActivity extends StatelessWidget {
  const TicketActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
